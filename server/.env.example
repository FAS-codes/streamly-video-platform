# Copy to .env and adjust as needed
PORT=5050
MONGO_URI=mongodb://127.0.0.1:27017/video_mern
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173

# Storage (local). Paths are relative to the server/ directory.
STORAGE_DIR=storage
MAX_UPLOAD_MB=500
